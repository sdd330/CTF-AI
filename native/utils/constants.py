"""
游戏常量定义
"""

# 游戏配置
TILE_SIZE = 32  # 每个格子的像素大小
DEFAULT_PRISON_DURATION = 20000  # 监狱持续时间（毫秒）
FPS = 60  # 帧率

# 颜色定义
COLOR_LEFT_TEAM = (0, 100, 200)  # 蓝色
COLOR_RIGHT_TEAM = (200, 0, 0)  # 红色
COLOR_WALL = (100, 100, 100)  # 灰色
COLOR_TARGET_L = (0, 150, 255)  # 浅蓝色（L队目标区）
COLOR_TARGET_R = (255, 100, 100)  # 浅红色（R队目标区）
COLOR_PRISON_L = (0, 50, 100)  # 深蓝色（L队监狱）
COLOR_PRISON_R = (150, 0, 0)  # 深红色（R队监狱）
COLOR_FLAG_L = (0, 200, 255)  # 亮蓝色（L队旗帜）
COLOR_FLAG_R = (255, 150, 150)  # 亮红色（R队旗帜）
COLOR_BACKGROUND = (240, 240, 240)  # 浅灰色背景
COLOR_GRID = (220, 220, 220)  # 网格线颜色
COLOR_TEXT = (0, 0, 0)  # 黑色文字

# 玩家配置
PLAYER_SIZE = 24  # 玩家大小（像素）
PLAYER_SPEED = 2  # 玩家移动速度（像素/帧）

# 旗帜配置
FLAG_SIZE = 20  # 旗帜大小（像素）

# 地图配置
MAP_MARGIN = 50  # 地图边距（像素）

